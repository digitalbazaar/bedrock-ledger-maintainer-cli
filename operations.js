const api = {};

api.createWitnessPoolDoc = async ({
  didDocument,
  nodes,
  maximumWitnessCount
}) => {

};

api.updateWitnessPoolDoc = async ({
  didDocument,
  existingWitnessPool,
  nodes,
  maximumWitnessCount
}) => {

};

module.exports = api;
